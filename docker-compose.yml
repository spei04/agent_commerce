services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: agent_commerce
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent
    ports:
      - "5432:5432"
    volumes:
      - agent_commerce_pg:/var/lib/postgresql/data

volumes:
  agent_commerce_pg:
